import { moderateScale } from '../utils/deviceConfig';

const metrics = {
  padding: moderateScale(16),
  margin: moderateScale(16),
  borderRadius: moderateScale(8),
  cardRadius: moderateScale(12),
  cardPadding: moderateScale(16),
  inputHeight: moderateScale(44),
  spacingSm: moderateScale(8),
  spacingMd: moderateScale(16),
  spacingLg: moderateScale(32),
  cardMaxWidth: moderateScale(400),
};

export default metrics; 